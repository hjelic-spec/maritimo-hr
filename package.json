{
  "name": "maritimo",
  "version": "0.1.0",
  "private": true,
  "description": "Maritimo — pomorska prognoza za voditelje brodica (Jadran)",
  "scripts": {
    "sync-www": "node scripts/sync-www.js",
    "cap:sync": "npm run sync-www && npx cap sync",
    "cap:open:android": "npm run sync-www && npx cap sync android && npx cap open android",
    "cap:open:ios": "npm run sync-www && npx cap sync ios && npx cap open ios",
    "dev": "python server.py"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/geolocation": "^6.0.1",
    "@capacitor/ios": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
